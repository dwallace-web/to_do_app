CREATE DATABASE pernapptodo;

CREATE TABLE todo(
    todo_id SERIAL PRIMARY KEY,
    details VARCHAR(255)
);

CREATE DATABASE pernapptodos;

CREATE TABLE todo(
    todo_id SERIAL PRIMARY KEY, 
    description VARCHAR(255)
);